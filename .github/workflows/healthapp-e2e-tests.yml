name: Health app E2E Tests

on:
  push:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '24'

      - name: Install healthapp dependencies
        working-directory: ./healthapp
        run: npm ci

      - name: Check healthapp TypeScript
        working-directory: ./healthapp
        run: npm run tsc

      - name: Start healthapp server
        working-directory: ./healthapp
        run: |
          npm start > server.log 2>&1 &
          echo $! > server.pid

      - name: Wait for healthapp server
        run: |
          for attempt in {1..30}; do
            if curl --fail --silent http://localhost:3000/hello > /dev/null; then
              exit 0
            fi
            sleep 1
          done
          cat healthapp/server.log
          exit 1

      - name: Install test dependencies
        working-directory: ./healthapp-tests
        run: npm ci

      - name: Install Playwright browsers
        working-directory: ./healthapp-tests
        run: npx playwright install --with-deps chromium

      - name: Run E2E tests
        working-directory: ./healthapp-tests
        run: npm test

      - name: Upload Playwright report
        uses: actions/upload-artifact@v5
        if: always()
        with:
          name: healthapp-playwright-report
          path: healthapp-tests/playwright-report/
          retention-days: 30

      - name: Stop healthapp server
        if: always()
        working-directory: ./healthapp
        run: |
          if [ -f server.pid ]; then
            kill "$(cat server.pid)" || true
          fi
